arbroath use something called sweet spot

barassie uses golf genius for opens
montrose uses golf genius for opens
newmachar use golf genius for opens
carnoustie use golf genius for opens
dornoch use golf genius for opens

look at mearns castle as they have prices differently

Need a better error message for courses that dont allow online booking but do allow opens

Need to checking bookingOpen flag on intelligent golf opens

look at this open https://howdidido-whs.clubv1.com/hdidbooking/open?token=II48002E14O714IOI0&cid=3664174&rnd=1

none of the dotgolf opens work

irvine does not use howdidido hub

moray new also has opens on it

dotgolf appears to have a competitions for this day section on the tee booker

-- mysql
UPDATE clubs
SET location = ST_SRID(POINT(lon, lat), 4326)
WHERE lat <> 0
AND lon <> 0;

-- mariadb
UPDATE clubs
SET location = ST_GeomFromText(CONCAT('POINT(', lon, ' ', lat, ')'), 4326)
WHERE lat <> 0
  AND lon <> 0;

Need to look at Carnoustie where all tee times are taken but still some slots free

            {
                "id": 3305175,
                "slot_date_time": "2026-04-10T15:00:00",
                "slot_mark": {
                    "id": 1,
                    "name": "Free"
                },
                "bookings": [
                    {
                        "players": 2
                    }
                ]
            },

dot golf open url;
https://fraserburghgolfclub.org/Teebooking/SearchSlots.aspx?ClubId=2262&CourseId=2060&Date=2026-05-02&CompetitionId=64617

34484
2026-07-05



Query to find opens with open booking but no opens

SELECT id, name, openBooking, openBookingSystem FROM `clubs` WHERE id NOT IN(select clubId from opens) AND openBooking = 'Yes';


Facebook
auchmill and ballumbie dont work despite the url being correct

SELECT id, name, confirmed_no_opens FROM `clubs` WHERE openBookingLink = '' AND openBookingSystem = '' AND confirmed_no_opens = '0';

Islay golf club opens not available until the day before the open - look at adding it anyway

Credits
<a href="https://www.flaticon.com/free-icons/golf" title="golf icons">Golf icons created by Good Ware - Flaticon</a>

look at open route service

prestonfield mapping info wrong

have a look at the phpunit gitlhub actions job to include phpunit

- name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.3
          tools: phpunit  # This puts phpunit in your PATH automatically

          Greenburn not in Pauls list
          killin not in pauls list